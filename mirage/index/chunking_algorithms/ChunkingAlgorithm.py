

class ChunkingAlgorithm:
    """
    A chunking algorithm is a procedure that creates a dict (index) of chunks from the text documents in the RawStorage
    """
    def __call__(document_raw_strings) -> dict[int, [str, str]]:
        pass